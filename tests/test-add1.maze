class test {
    void main (string args) {
        Addr addr = new Addr();
        int result = addr.add(1+2);
        print(result);
    }
}

class Adder {
    int add(int x, int y)
    {
        return x + y;
    }
}
